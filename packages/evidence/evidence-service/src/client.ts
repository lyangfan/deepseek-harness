/**
 * Client-namespace projection of the Evidence read domain: a pure re-export of the package's
 * types outlet (single source, zero duplication).
 *
 * @module @deepseek-ai/dsh-evidence-service/client
 */

export type * from './types.ts'
